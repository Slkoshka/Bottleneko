import users from '../users/api';
import connections from '../connections/api';
import scripts from '../scripts/api';
import proxies from '../proxies/api';
import system from '../system/api';

export default {
    users,
    connections,
    scripts,
    proxies,
    system,
};
